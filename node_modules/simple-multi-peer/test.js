var SimpleMultiPeer = require('./lib');

var Foo = new SimpleMultiPeer({
  server: 'ws://localhost:3000',
  room: 'foobar',
  peerOptions: {wrtc: require('wrtc')},
});

setTimeout(function() {
  Foo.send({foo: 'bar'})
}, 2000);
